/*
 * This is an example of an interface.
 * Particularly, this one will be used in the app.ts when sending a message response
 * after hitting our / endpoint.
 * It only has the message variable whose type is of 'string'
 * 😇 Easy peasy, 🍋 lemon-squeezey 👍
 */
export interface MessageResponse {
    message: string;
}
